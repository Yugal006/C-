#include<stdio.h>

int main(){
    // int a, b;
    // printf("enter a length of reactangle: ");
    // scanf("%d", &a);
    // printf("enter a breath of reactangle: ");
    // scanf("%d", &b);
    // printf("The area of rectangle whose length %d and %d is : %d.",a ,b, a*b); 
    // return 0;
    float c;;
    printf("type tha temperature in celcius :");
    scanf("%f", &c);
    printf("The %f degree celcius is equal to: %f",c , (c*1.8)+32);
    return 0;
}