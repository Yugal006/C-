#include<stdio.h>

int main(){
    // int a = 5;
    // int b = 10;
    // int c = a + b;
    // printf("the value of a is %d the value of b is %d and sum of them is %d and product of them is %d", a, b, c, a*b);

    //modulus of any numer is simply means its is a remainder of them i.e
    // printf("\nthe modulus of %d and %d is %d", a, b, a%b);
    int b = 2, c = 3;
    int z; z = b*c; //legal
    // int z; b*c = z; illegal (not allowed)
    // % → cannot be applied on float
    // % → sign is same as of numerator (-5%2=-1)
    // An Arithmetic operation between
    // • int and float → float
    // • float and float → float
    // • int and int → int

    int v =3*3;
    printf("%d", v);
    return 0;
}