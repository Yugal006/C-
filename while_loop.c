#include<stdio.h>

int main(){
    // float i = 1;
    // while (i<=2)
    // {
    //     printf("%f\n", i);
    //     i = i + 0.1;
    //     // i++;
    // }

    // some imp operators +=, -=, *=, /=, %=.
    int i = 0;
    while (i<=20)
    {   
        if (i>=10)
        {
            printf("%d\n",i);
        }
        i += 1;
        // printf("%d\n", i);
        // // i = i + 1;
        // i++;
    }
    return 0;
}